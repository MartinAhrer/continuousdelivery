apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresdb-data
  labels:
    app: postgresdb
spec:
  storageClassName: local
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
