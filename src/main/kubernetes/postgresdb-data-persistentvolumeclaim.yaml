apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgresdb-data-pvc
  labels:
    app: postgresdb-data
spec:
  storageClassName: local-storage
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
